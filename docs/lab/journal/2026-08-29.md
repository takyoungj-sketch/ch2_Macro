# 2026-08-29

## 작업
- 전국 지역 순위 카드(D-053): 같은 grain 전국, 3탭, pin, 검색=스크롤, 가상 리스트. 특화도 %p
- 순위 표에서 `위` 제거(칸 겹침). 하단 한 줄만 `위` 유지
- 전국 분포 산점도(F3): 인구×탭값 로그, 현재 지역 점, 클릭=목록 이동
- D-054: 프로필=완결 달력 3년 · 연초 1회. 월간 사이클에 넣지 않음
- D-055: M2·금리 상관은 전국 시계열만. 지역 상관은 인구·유형. 뉴스판 금지
- G1 인구-거래 동조 r(산점도 로그 Pearson). G2 유형 동조 8×8 비중 행렬(금액/건수)

## 검증
- pytest `test_profile_national_rank.py` 9 passed
- 로컬 `build_regional_profile_rank.py` 26594행. 전국 스냅샷 `as_of=2026-06-01`(시군구 258). `2026-07-01`은 부분(시군구 46)
- API `GET /api/regional-profile/national-ranks` 200 + `type_corr`
- 화면 브라우저 자동화는 없음. 로컬 `http://localhost:5177/profile/`

## 중지 지점
G3 전. 내일:

1. 커밋·배포는 **요청 있을 때만**. 범위는 프로필 축(아래 파일). molit zip·`_tmp*`·`.env` 넣지 말 것
2. G3: **전국** 월별 총거래액·유형별 총액 ↔ M2 증가율 · 대표 시장금리 **하나**. 지역 r 없음. ECOS 출처·갱신 주기는 프로필 연초(D-054)와 분리
3. 전국 프로필을 7월 as_of로 맞출지는 별도(지금 7월은 부분 재빌드)

SSOT: `docs/PROFILE_NATIONAL_RANK_PLAN.md` · `docs/REGIONAL_PROFILE_ARCHITECTURE.md` §12.8–12.10 · D-053·054·055

커밋에 넣을 핵심:
- `db/070_regional_profile_rank.sql`
- `pipeline/build_regional_profile_rank.py` (+ `build_regional_profile.py` 훅)
- `backend/app/regional_profile/national_ranks.py` · `router.py` · `main.py`(gzip) · tests
- `frontend-profile` 순위/산점도/유형동조 · `shared/stats-glossary/statsGlossary.ts`
- `docs/DECISIONS.md` · `docs/lab/decisions/D-053.json`–`D-055.json` · 체크리스트 §7 · 아키텍처

## 커밋
- 토지: —
- 복합: —
- 집합: —
- 임대: —
- 지역프로필: 미커밋 — 순위·산점도·동조 r·유형 동조 · D-053/054/055
- 관리: 미커밋 — 계획일지 08-29
