# 2026-08-30

## 작업
- D-056 확정: 5층 계약. Planner=경로+실행 가능. History=성공 Bundle만. Gate≠Caveat. 투자·적정가 금지. 코호트 프리필 없음
- P0–P2에 이어 P3 실행 보조: `navigate` / `open_ui` / `run_engine`(승인 후 기존 엔진만). History 비교. 메모 후속 칩
- 사다리: 집합=깊게(코호트 UI → 승인 `run_engine` → `ch2-ai-engine-done` 해석). 복합=통계분석 스크롤·승인. 토지/프로필=이동·스크롤. 임대=헤더 AI만
- 사용법 질문(「어떻게 보나」)은 Playbook 경로 가로채기 금지 → 화면 클릭 순서 (`9569d38`)
- 헤더 AI 한 버튼. 카드 안 중복 제거
- AI·분석방법 `?`·지표 `?` 창: 제목줄 이동, 8방향 크기 조절, 크기(AI는 위치 포함) localStorage. **워킹트리**

## 검증
- `backend/tests/test_ai_planner.py` (P3·사용법 커밋에 포함)
- 창 크기조절: 브라우저 클릭 검증 없음. 공유 경로 `shared/ui-window/resizableWindow.tsx`

## 중지 지점
이어서: [`docs/CH2_AI_ASSISTANT_EXPANSION_PLAN.md`](../../CH2_AI_ASSISTANT_EXPANSION_PLAN.md) **§12**.

1. 창 크기조절 **커밋·배포는 요청 있을 때만**. 범위는 공유 패널(아래). `_tmp*`·`.env` 넣지 말 것
2. 브라우저로 AI·분석 `?`·지표 `?` 크기 유지·Esc/바깥클릭 확인 (집합 한 앱이면 공유라 주거·비주거 같이)
3. P4 메모 다듬기: `format_memo`는 있음. `user_override` 문장·슬롯 상한·TTL은 D-056 revisit
4. P5 깊게(복합/토지/프로필 승인 엔진)는 집합 사다리 유지 후. 지금은 scroll/navigate
5. 임대: 표 n·P50·환산칸을 AI context로. 전환율 잠금문이 칸 공란을 덮지 않게
6. 하지 말 것: 코호트 프리필, LLM 숫자 invent, 백그라운드 회귀로 경로 「검증」

커밋에 넣을 창 크기조절(요청 시):
- `shared/ui-window/resizableWindow.tsx` (신규)
- `shared/ai-assistant/AiAssistantPanel.tsx`
- `shared/analysis-help/AnalysisHelpPanel.tsx`
- `shared/stats-glossary/StatsGlossaryHelp.tsx` · `glossaryHelp.css`

이미 main: `904bb12` P3·헤더 AI, `9569d38` 사용법 질문

## 커밋
- 토지: P3 얕음 `904bb12`. 창 크기조절은 08-31
- 복합: P3 얕음 `904bb12`. 창 크기조절은 08-31
- 집합: P3·사용법 `904bb12`·`9569d38`. 창 크기조절은 08-31
- 임대: 헤더 AI `904bb12`. 전세/환산 표·창은 08-31
- 지역프로필: P3 얕음 `904bb12`. 창 크기조절은 08-31
- 관리: 일지 08-30은 08-31 커밋에 포함
