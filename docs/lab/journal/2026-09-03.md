# 2026-09-03

## 작업
- 지역프로필 지역선택 저비용(D-057): 리가 없는 법정동(`…00`, 동명=리명)은 읍면동 8자리로 연다. 가능동 검색·북마크·토지→프로필 링크. 제안 배지 「리」는 실제 리만. 헤더·검색창 동명 중복 제거
- 토지 `RegionSelector` 칩·Enter는 유지. 딥링크만 예외
- 근본(리 순위 마트에서 동 제외, 프로필 `beopjungri` `…00` 행 미생성, `region_codes` 삭제 금지)은 [`PROFILE_NATIONAL_RANK_PLAN.md`](../../PROFILE_NATIONAL_RANK_PLAN.md) §9 · 계획표 next

- 리 전국 순위 저비용 필터: `national-ranks`가 `beopjungri`일 때 `…00`을 빼고 거래액·건수·인구대비 위를 다시 매김. 마트 재빌드 없음
- 승격 되돌림: 읍면동 프로필이 없는 동(전국 3곳, 예 삼척 사직동 `5123011600`)은 원래 10자리로 복귀
- 리 Twin 카드 같은 필터(개선점 2): `/twins-beop`가 `…00` 후보를 빼고 `#`를 1부터 다시 매김. 앵커 자신이 `…00`(되돌림 3곳)이면 필터 없음. 마트 재빌드 없음

## 검증
- `tsx` 헬퍼: 가능동 `4115010100` → 읍면동 `41150101`. 옥천읍 마암리 10자리는 리 유지. 제안에서 동은 읍면동 줄, 리는 리 줄
- `frontend-profile`·`frontend` `tsc --noEmit` 통과 · `pytest tests/test_profile_national_rank.py` 10 passed
- 로컬 API 실측(`as_of=2026-06-01`): 리 마트 18,605행 중 `…00` 3,564행 → 순위 응답 15,041곳, `…00` 0건. 읍면동 5,038 · 시군구 258
- 남이면 가좌리 `4311231036` 리 순위 818위. 가능동 `41150101`은 읍면동 254위, 남이면 `43113250` 352위
- `…00` 3,564곳 중 3곳만 읍면동 프로필이 없음 → 되돌림으로 처리
- Twin: 가좌리 `4311231036` 후보에서 강내동 `4311210500`(원래 #3) 빠지고 신월리·석실리 #1·#2. 강내동 앵커로 열면 그대로 3곳. 리 앵커 14,995곳 중 후보가 전부 동인 76곳은 빈 카드 → §9에서 해소

## 중지 지점
1. 배포는 요청 있을 때만. 운영 반영 안 됨
2. Twin 후보가 전부 동인 76곳 빈 카드 · 특화도 mix 분모의 동 포함은 §9 마트 재빌드 때
3. G3 유동성·금리 동조는 별도

## 커밋
- 토지: 커밋 — 프로필 딥링크 `…00`→읍면동 (`bb1abc8`)
- 복합: —
- 집합: —
- 임대: —
- 지역프로필: 커밋 — 검색·URL 승격 · 라벨 중복 제거 · 리 순위 필터 (`bb1abc8`) / 리 Twin 필터는 미커밋
- 관리: 미커밋 — D-057 Twin 필터 추가 · 순위계획 §9 · 글로서리 · 일지 09-03
