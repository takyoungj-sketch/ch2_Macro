/** 레거시 클래스 문자열 — 크기·드래그는 DraggableModalShell이 담당. 참조용으로 유지. */
export const COLLECTIVE_DETAIL_MODAL_SHELL_CLASS =
  "fixed left-1/2 top-1/2 modal-shell rounded-xl shadow-xl max-w-5xl w-[min(calc(100%-2rem),64rem)] max-h-[90vh] min-h-[min(70vh,640px)] flex flex-col border";
