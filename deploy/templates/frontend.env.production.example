# CH2 Macro — frontend 빌드용 .env
# cp deploy/templates/frontend.env.production.example frontend/.env
# backend API_TOKEN 과 동일 값 → npm run build

VITE_API_TOKEN=CHANGE_ME_API_TOKEN

# 운영/dev VPS 에서는 비움
# VITE_STATS_V2_ASSUMED_TODAY=
